events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name localhost;

        location /stub_status {
            stub_status on; # 关键！开启状态模块
            allow all;      # 允许所有IP访问（仅限内网，生产环境要设置权限）
        }
    }
}

